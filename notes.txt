Hack for covariance:
(x1 - u)(x1 - u)

compute for each x and then calculate the average (or skewed average dividing by N - 1)


Entropy:
-(freq * log(freq) + ..)

Impurity:
1 - freq**2 - freq**2 - ...